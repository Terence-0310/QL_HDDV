export default function ContractsPage() {
  return (
    <main style={{ maxWidth: "900px", margin: "2.5rem auto", padding: "0 1rem" }}>
      <section className="card" style={{ padding: "1.2rem" }}>
        <h2 style={{ marginTop: 0 }}>Hợp đồng</h2>
        <p style={{ color: "var(--text-muted)" }}>Màn hình này được giữ cho luồng mở rộng trong các phiên bản tiếp theo.</p>
      </section>
    </main>
  );
}
